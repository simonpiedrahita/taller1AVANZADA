let temperaturaMaxima = 30;
let temperaturaMinima = 15;
function calcularTemperaturaMediaTradicional(temperaturaMaxima, temperaturaMinima) {
    return (temperaturaMaxima + temperaturaMinima) / 2;
  }
  // Ejemplo de uso

  let temperaturaMedia = calcularTemperaturaMediaTradicional(temperaturaMaxima, temperaturaMinima);
  console.log('La temperatura media es:', temperaturaMedia,'\n La temperatura maxima es:', temperaturaMaxima,'\n La temperatura maxima es:',temperaturaMinima);
  